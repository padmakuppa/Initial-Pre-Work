//I learned about Math.random() function, and it would generate a random decimal number between 0(inclusive)-1(exclusive).
function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
