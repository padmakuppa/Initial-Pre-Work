//I learned that if a function does not have a ‘return’ statement, the function process the inner code but the result would be undefined.
// Example
var sum = 0;
function addThree() {
  sum = sum + 3;
}

// Only change code below this line
function addFive() {
   sum = sum + 5;
}


// Only change code above this line
var returnedValue = addF
