//I learned about parseInt() function’s second argument radix, and radix can be an integer between 2 and 36. 
function convertToInteger(str) {
return  parseInt(str,2);
}

convertToInteger("10011");
