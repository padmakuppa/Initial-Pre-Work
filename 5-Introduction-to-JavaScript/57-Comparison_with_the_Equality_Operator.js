//I learned about the comparison operator == in Javascript, and the equality operator would return Boolean values.
// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);
